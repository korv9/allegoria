# Prediction: normative-structure axis

Date: 2026-09-11. Status: pre-run draft, NOT git-timestamped preregistration.
No transformation or extraction calls were made. The source-only marker audit
has already informed this document; it is not a preregistered discovery.
Corpus version: `philosophy_v1`, SHA-256
`21264cc24e2edad4ae772457c49b1d0400d0a3c529b0d032732d0906e5725510`.
Statutory audit snapshot SHA-256:
`de9269320e2d227920d92b147cfcd726110a19d81a251e7cb3b2ae33ce658475`.
Commit this file and freeze corpus/code hashes before the first real run, as
required by PROTOCOL.md. The notebook reports the current corpus SHA-256.

## Fixed future comparisons

- P1: descriptive virtue passages remain free of explicit deontic slots through
  generations 0–10. Their empty slot vectors have no directional events (not an
  invented neutral slot). Any adjudicated emergent duty/permission falsifies
  the textual invariance prediction. A signal on text still independently judged
  purely descriptive is an instrument failure; these are separate outcomes.
- P2: at least half the categorical passages acquire an exception to their
  original duty by generation 3, retaining an identifiable duty. This is an
  explicit working hypothesis, not a power-backed estimate. Fewer than half
  falsifies the timing/coverage claim in that model/prompt stratum.
- P3: by generation 3, the proportion of categorical chains with a **new distinct
  escape condition** exceeds that of their paired conditional chains. In both
  groups the denominator is all assigned chains, not only successful outputs.
  The control's existing exception, its rephrasing, and mere weakening are not
  new insertions. Duty deletion is a separate loosening event, not an insertion.

Use the same ten topics, chain depth, transform style, sampling settings and
replicate count in each paired stratum. Report models, prompt variants and
transform styles separately; do not pool away reversals. At least two transformer
models and two neutral prompt variants are required by PROTOCOL.md. Exact model
versions, prompt files, replicate count and an uncertainty analysis remain
UNCONFIRMED: lock these in an additional pre-run commit before any model calls.
This draft alone does not authorize a run.

## Falsification and accepted null

P3 is directionally contradicted by an observed categorical-minus-control
insertion proportion at or below zero. If uncertainty cannot distinguish the
rates, report “no evidence beyond generic hedging”; do not claim proven equality
or that hedging is the uniquely established cause. Report the small sample and
uncertainty. A positive point estimate alone is not sufficient confirmation.
Refusals, truncations and extraction failures remain visible as missing outcomes;
report lower/upper insertion bounds treating unresolved cases both ways.

Do not count absent source exceptions as infinitely many available empty slots.
Hand-define duty identity and distinguish a newly inserted escape clause from a
qualifier attached to an already present exception before implementing direction.
The current DIRECTION.md has no complete executable insertion contract.

## Analysis and demo, when data exist

- Plot loosening events per parent→child generation, with equal exposure shown;
  if also cumulative, explicitly label that different curve. Generation zero is
  the source, not a measured transformation. Never subtract tightening.
- Report first qualifier-absence generation with surviving chains right-censored.
- Report authentic/fictional differences only for actual annotated statutory twins.
- Blind human review of at least 20% of extractions; agreement per slot kind,
  with sample sizes and disagreements, not a fabricated aggregate score.
- Choose the illustrative trajectory by a declared rule before looking at runs:
  first eligible passage ID with complete provenance, display every generation.
  A categorical-to-conditional example supports that textual transition, not
  the claim that an entire ethical theory changed.

No simulated outcomes, curves or agreement figures are permitted.
